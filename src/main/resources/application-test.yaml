# application-test.yaml
spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

custom:
  jwt:
    secret-key: test-secret-key-for-ci-only-must-be-at-least-32-bytes-long-1234
    expire-seconds: 600